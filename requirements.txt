# Installation note: pyyaml 5.4.1 (required by PL 1.3.8) fails to build.
# Workaround: first install pyyaml>=6.0, then install pytorch-lightning==1.3.8 --no-deps
# Example: uv pip install "pyyaml>=6.0" && uv pip install pytorch-lightning==1.3.8 --no-deps

# Standard
numpy==1.26.4
matplotlib
seaborn
pandas

# Code quality
mypy
pytest
flake8
brunette

# Learning libraries
torch==1.13.1 
torchvision==0.14.1 
torchaudio==0.13.1
pytorch-lightning==1.3.8  # Pinned for checkpoint compatibility
torchmetrics==0.5.1
# Original versions (changed due to Apple silicon)
#torch==1.9.1
#pytorch-lightning==1.3.4
#torchmetrics==0.5.1


# Optimization
cvxpy==1.2.1
#cvxpylayers==0.1.5 #BROKEN
gurobipy==9.1.2
osqp
ecos
scs
casadi

# Geometry
shapely

# Freeze versions to avoid bugs
setuptools==59.5.0
scipy==1.10.1
